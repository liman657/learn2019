<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.learn.mybatisplus.dao.UserMapper">

    <select id="selectSelfSQL" resultType="com.learn.mybatisplus.entity.UserEntity">
        select * from user ${ew.customSqlSegment}
    </select>

    <select id="selectSelfPage" resultType="com.learn.mybatisplus.entity.UserEntity">
        select * from user ${ew.customSqlSegment}
    </select>
</mapper>
